/*
 * --------------------------------------------------
 *  Config file for defining DSL2 per module options
 * --------------------------------------------------
 *
 *   Available keys to override module options:
 *       args           = Additional arguments appended to command in module.
 *       args2          = Second set of arguments appended to command in module (multi-tool modules).
 *       publish_dir    = Directory to publish results.
 *       publish_by_id  = Publish results in separate folders by meta.id value.
 *       publish_files  = Groovy map where key = "file_ext" and value = "directory" to publish results for that file extension
 *                        The value of "directory" is appended to the standard "publish_dir" path as defined above.
 *                        If publish_files == null (unspecified)  - All files are published.
 *                        If publish_files == false               - No files are published.
 *       suffix         = File name suffix for output files.
 *
 */

params {
    modules {
        'run_mode' {
            run_identification = true
            run_quantification = false
            run_differential = false

            // star index process
            // only set to true if it hasn't been done yet for this genome, this process takes around 25 minutes
            run_star_genome_generate = false
        }
        'output_files' {
            output_dir = "challenges_outputs"

            identification_out_suffix = 'identificaion_output.bed'
            quantification_file_out = 'isoscm_quantification_output.bed'
            differential_file_out = 'isoscm_differential_output.tsv'
        }
        'check_samplesheet' {
            publish_dir   = 'public_data'
        }
        'files' {
            gtf_genome_file = '/home/fzhuang/apaeval/APAeval/execution_workflows/IsoSCM/../../tests/test_data/gencode_2genes_Chr_prefix.vM18.annotation.gtf'
            fasta_genome_file = '/home/fzhuang/apaeval/APAeval/execution_workflows/IsoSCM/../../tests/test_data/GRCm39.primary_assembly.genome.fa'
            star_genome_index = '/home/fzhuang/apaeval/APAeval/execution_workflows/IsoSCM/results/isoscm/star_index'

        }
    }
}
