# Adjust METHOD_NAME and add/remove packages as needed.
name: METHOD_NAME
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.9
  - numpy
  - scipy
#  - [OTHER_PACKAGE]
  - pip
  - pip:
#    - [PIP_PACKAGE]