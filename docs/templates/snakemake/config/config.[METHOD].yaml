# Configuration file for snakemake pipeline

# Dataset samples
samples: "config/[SAMPLES].csv"

# For development purposes, you can freely choose out_dir.
out_dir: "results"
param_code : "PARAM_CODE"
method: "METHOD"
# Note that final outputs need to be copied to a global APAeval output directory in the form of "PATH/TO/s3-BUCKET/PARAM_CODE/METHOD/"

# Parameters
# define method specific parameters here

# Settings
envs: "workflow/envs"
local_log: "logs/local_log"
cluster_log: "logs/cluster_log"